using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class TerrainGenerationUI : MonoBehaviour
{
/*    [SerializeField] private TMP_InputField seedInput;
    [SerializeField] private Button generateTerrainButton;
    [SerializeField] private GameObject terrainGeneratingIndicator;

    [SerializeField] private TerrainManager terrainManager;

    private void Awake()
    {
        generateTerrainButton.onClick.AddListener(() =>
        {
            OnGenerateTerrain(seedInput.text);
        });
    }

    private void OnGenerateTerrain(string input)
    {
        int seed = int.Parse(input);
        terrainGeneratingIndicator.SetActive(true);
        terrainManager.GenerateTerrain(seed, this);
    }

    public void DoneLoading()
    {
        gameObject.SetActive(false);
    }*/
}
